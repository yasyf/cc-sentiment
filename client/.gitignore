# Rust build artifacts
target/
*.rlib
*.rmeta
*.pdb

# Compiled extension (built in-place by editable installs)
cc_sentiment/_transcripts_rs*.so
cc_sentiment/_transcripts_rs*.pyd
cc_sentiment/_transcripts_rs*.dylib

# Python build artifacts
dist/
build/
*.egg-info/
*.egg

# Hand-labeled dev dataset (ground truth for prompt iteration; not committed)
cc_sentiment/labels.jsonl
