# [bdist_wheel] py_limited_api is the canonical abi3 source per setuptools-rust
# (build.py:444-450). pyproject [tool.distutils.bdist_wheel] is not stable — see
# pypa/wheel#582.
[bdist_wheel]
py_limited_api = cp313
